import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  inputContainer: {
    width: "80%",
    marginVertical: 10,
    borderWidth: 1,
    borderColor: "gray",
    padding: 10,
    borderRadius: 20,
    fontSize: 20,
  },
});
